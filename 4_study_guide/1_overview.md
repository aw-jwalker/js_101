# JS109 Study Guide

## This should be a heading h2

```javascript
function example() {
  for (let i = 0; i < 10; i++) {
    console.log("hi");
  }
}
```

## naming conventions

1. legal vs idiomatic
2. illegal vs non-idiomatic

## primitive values vs objects

## type coercions

1. explicit and implicit

## numbers

1. types of numbers (`float` and `int`)
2. dont forget `NaN` and `infinity`

## strings

## template literals

## boolean vs truthy

## undefined and null

## array and object syntax

## array properties and methods

1. `array.length`
2. `array.push`
3. `array.pop`
4. `array.reverse`

## object methods

1. `object.keys`
2. `object.values`
3. `object.entries`

## operators

1. numeric: `+`, `-`, `*`, `/`, `%`
2. string: `+`
3. conditional: `===`, `!==`, `<`, `>`, `>=`, `<=`, `==`, `!=`, `ternary (? :)`
4. loose and strict equality (`===` vs `==`)
5. logical operators and short circuit eval (`!`, `&&`, `||`)
6. `typeof` operator
7. operator precedence

## mutability, immutability, `const`

## variables

1. identifier:
  a. variable names:
  b. constant names:
  c. property names:
  d. function names:
2. variable and constant declarations
3. initialization, assignment, reassignment
4. scope
5. variables as pointers
6. variable shadowing

## conditionals and loops

## `console.log`

## `readline-sync` and the `question` method

## `require`

## functions

1. functions vs methods
2. declarations, expressions, arrow functions
3. defintion and invocation
4. default parameters
5. return values
6. parameters vs arguments
7. function, block, local, global scope
8. nested functions
9. function composition
10. output vs return values, side effects
11. *pass-by-reference* and *pass-by-value*

## expressions and statements

## exceptions: throwing and catching

## common `Math` methods

1. `Math.floor`
2. `Math.random`
3. `Math.pow`

## function user-level description vs implementation

> this is a block quote?
> now its multiple lines
> > this is a nested block quote?
